@model IEnumerable<DateTime>

@if (Model.Any())
{
    <div>
        <i>Missing Sadhana Records: Click above links for previous months.</i>
        <ul>
            @foreach (var date in Model)
            {
                <li>@date.ToShortDateString()</li>
            }
        </ul>
    </div>
}
